#pragma once

/**
 * @file CrosspadPcVersion.hpp
 * @brief CrossPad PC simulator version
 *
 * Keep in sync with project(VERSION) in CMakeLists.txt.
 * CI release workflow overrides this via cmake configure_file.
 */

#define CROSSPAD_PC_VERSION "0.2.6"
